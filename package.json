{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "telemetry": "turbo run telemetry",
    "update": "turbo run update",
    "upgrade": "turbo run upgrade"
  },
  "devDependencies": {
    "eslint": "8.8.0",
    "npm-check-updates": "^12.3.0",
    "prettier": "^2.5.1",
    "turbo": "^1.1.2"
  },
  "packageManager": "pnpm@6.30.0",
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
